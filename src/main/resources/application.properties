# ═══════════════════════════════════════════════════════
# Impact Analysis Service — Configuration
# ═══════════════════════════════════════════════════════

# ── Application ─────────────────────────────────────────
quarkus.application.name=impact-analysis-service
quarkus.application.version=1.0.0

# ── HTTP Server ─────────────────────────────────────────
quarkus.http.port=8083
quarkus.http.test-port=8085

# ── PostgreSQL ──────────────────────────────────────────
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${POSTGRES_USER:airline_user}
quarkus.datasource.password=${POSTGRES_PASSWORD:airline_pass_2024}
quarkus.datasource.jdbc.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:airline_ops_db}?sslmode=require
quarkus.datasource.jdbc.max-size=10
quarkus.datasource.jdbc.min-size=2

# ── Hibernate ───────────────────────────────────────────
quarkus.hibernate-orm.database.generation=none
quarkus.hibernate-orm.log.sql=false
quarkus.hibernate-orm.physical-naming-strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy

# ── Kafka Consumer (disruption_events) ──────────────────
kafka.bootstrap.servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

mp.messaging.incoming.disruption-events-in.connector=smallrye-kafka
mp.messaging.incoming.disruption-events-in.topic=disruption_events
mp.messaging.incoming.disruption-events-in.value.deserializer=org.apache.kafka.common.serialization.StringDeserializer
mp.messaging.incoming.disruption-events-in.group.id=impact-analysis-group
mp.messaging.incoming.disruption-events-in.auto.offset.reset=earliest

# ── Kafka Producer (impact_events) ──────────────────────
mp.messaging.outgoing.impact-events.connector=smallrye-kafka
mp.messaging.outgoing.impact-events.topic=impact_events
mp.messaging.outgoing.impact-events.value.serializer=org.apache.kafka.common.serialization.StringSerializer
mp.messaging.outgoing.impact-events.key.serializer=org.apache.kafka.common.serialization.StringSerializer

# ── Redis ───────────────────────────────────────────────
quarkus.redis.hosts=redis://${REDIS_HOST:localhost}:${REDIS_PORT:6379}
quarkus.redis.password=${REDIS_PASSWORD:airline_pass_2024}

# ── Dev Services deshabilitados ──────────────────────────
quarkus.devservices.enabled=false
quarkus.redis.devservices.enabled=false
quarkus.datasource.devservices.enabled=false
quarkus.kafka.devservices.enabled=false

# ── OpenAPI ─────────────────────────────────────────────
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/swagger-ui
mp.openapi.extensions.smallrye.info.title=Impact Analysis Service API
mp.openapi.extensions.smallrye.info.version=1.0.0

# ── Logging ─────────────────────────────────────────────
quarkus.log.level=INFO
quarkus.log.category."com.airline".level=DEBUG
quarkus.log.console.format=%d{HH:mm:ss} %-5p [%c{2.}] %s%e%n

# ── Dev Profile ─────────────────────────────────────────
%dev.quarkus.hibernate-orm.log.sql=true
%dev.quarkus.log.level=DEBUG

# ── Kafka SASL/SSL para Upstash ──────────────────────────
kafka.security.protocol=SASL_SSL
kafka.sasl.mechanism=SCRAM-SHA-256
kafka.sasl.jaas.config=org.apache.kafka.common.security.scram.ScramLoginModule required username="${KAFKA_SASL_USERNAME}" password="${KAFKA_SASL_PASSWORD}";

mp.messaging.connector.smallrye-kafka.security.protocol=SASL_SSL
mp.messaging.connector.smallrye-kafka.sasl.mechanism=SCRAM-SHA-256
mp.messaging.connector.smallrye-kafka.sasl.jaas.config=org.apache.kafka.common.security.scram.ScramLoginModule required username="${KAFKA_SASL_USERNAME}" password="${KAFKA_SASL_PASSWORD}";

quarkus.http.cors=true
quarkus.http.cors.origins=https://airline-platform.vercel.app
quarkus.http.cors.methods=GET,POST,PATCH,PUT,DELETE,OPTIONS
quarkus.http.cors.headers=Content-Type,Authorization,Accept